# 🏗️ Architecture Overview

## Application Structure

```
┌─────────────────────────────────────────────────────────────┐
│                     Next.js App Router                       │
│                     (app/ directory)                         │
└─────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┼─────────────┐
                │             │             │
                ▼             ▼             ▼
        ┌──────────┐  ┌──────────┐  ┌──────────┐
        │ layout   │  │  page    │  │ globals  │
        │  .tsx    │  │  .tsx    │  │  .css    │
        └──────────┘  └──────────┘  └──────────┘
                │
                ▼
        ┌──────────────────────────┐
        │   Client Component       │
        │     (src/App.tsx)        │
        └──────────────────────────┘
                │
    ┌───────────┼───────────┐
    │           │           │
    ▼           ▼           ▼
┌────────┐  ┌────────┐  ┌────────┐
│ Vehicle│  │ Staff  │  │Shift   │
│  Mgmt  │  │  Mgmt  │  │ Mgmt   │
└────────┘  └────────┘  └────────┘
    │           │           │
    └───────────┼───────────┘
                │
                ▼
        ┌──────────────┐
        │  Components  │
        │   (62 files) │
        └──────────────┘
                │
    ┌───────────┼───────────┐
    │           │           │
    ▼           ▼           ▼
┌────────┐  ┌────────┐  ┌────────┐
│   UI   │  │  Lib   │  │ Types  │
│shadcn  │  │ Utils  │  │   TS   │
└────────┘  └────────┘  └────────┘
```

---

## PWA Layer

```
┌─────────────────────────────────────────┐
│         Progressive Web App             │
└─────────────────────────────────────────┘
              │
   ┌──────────┼──────────┐
   │          │          │
   ▼          ▼          ▼
┌──────┐  ┌──────┐  ┌──────┐
│Manif.│  │Serv. │  │Offln.│
│.json │  │Worker│  │.html │
└──────┘  └──────┘  └──────┘
```

---

## Data Flow

```
User Interaction
      │
      ▼
┌───────────┐
│ Component │
└───────────┘
      │
      ▼
┌───────────┐
│   State   │
│ (useState)│
└───────────┘
      │
      ▼
┌───────────┐
│ Mock Data │
│   (lib/)  │
└───────────┘
      │
      ▼
┌───────────┐
│   Render  │
└───────────┘
```

---

## Build Pipeline

```
Developer Push
      │
      ▼
┌────────────┐
│   GitHub   │
│  Actions   │
└────────────┘
      │
   ┌──┴──┐
   │     │
   ▼     ▼
┌────┐ ┌────┐
│Lint│ │Type│
└────┘ └────┘
   │     │
   └──┬──┘
      ▼
┌──────────┐
│   Build  │
│ (Turbo)  │
└──────────┘
      │
      ▼
┌──────────┐
│  Artifact│
└──────────┘
```

---

## Deployment Flow

```
Local Development
      │
   npm run build
      │
      ▼
┌─────────────┐
│  .next/     │
│  (output)   │
└─────────────┘
      │
  vercel --prod
      │
      ▼
┌─────────────┐
│   Vercel    │
│   Edge      │
└─────────────┘
      │
      ▼
┌─────────────┐
│  Production │
│     CDN     │
└─────────────┘
```

---

## Tech Stack Layers

```
┌─────────────────────────────────────┐
│        Presentation Layer           │
│  React 19 + Next.js 16 App Router  │
└─────────────────────────────────────┘
              ▲
              │
┌─────────────────────────────────────┐
│         Component Layer             │
│    shadcn/ui + Radix UI + Lucide   │
└─────────────────────────────────────┘
              ▲
              │
┌─────────────────────────────────────┐
│          Styling Layer              │
│         Tailwind CSS v4             │
└─────────────────────────────────────┘
              ▲
              │
┌─────────────────────────────────────┐
│          Type Layer                 │
│      TypeScript (strict mode)       │
└─────────────────────────────────────┘
              ▲
              │
┌─────────────────────────────────────┐
│          Build Layer                │
│    Next.js Turbopack + PostCSS     │
└─────────────────────────────────────┘
```

---

## Key Features Map

```
┌───────────────────────────────────────────┐
│            CarWash Pro App                │
└───────────────────────────────────────────┘
                    │
    ┌───────────────┼───────────────┐
    │               │               │
    ▼               ▼               ▼
┌─────────┐   ┌─────────┐   ┌─────────┐
│ Vehicle │   │  Staff  │   │  Team   │
│  Mgmt   │   │  Mgmt   │   │  Chat   │
└─────────┘   └─────────┘   └─────────┘
    │               │               │
    ▼               ▼               ▼
┌─────────┐   ┌─────────┐   ┌─────────┐
│  Shift  │   │  Leave  │   │ Health  │
│  Mgmt   │   │ Request │   │  Check  │
└─────────┘   └─────────┘   └─────────┘
    │               │               │
    └───────────────┼───────────────┘
                    ▼
            ┌──────────────┐
            │   Reports    │
            │  Analytics   │
            └──────────────┘
```

---

## File Organization

```
Project Root/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # Root layout + PWA
│   ├── page.tsx           # Home page
│   └── globals.css        # Tailwind imports
│
├── components/            # React components
│   ├── ui/               # shadcn/ui (50+ files)
│   ├── lib/              # Utilities
│   │   ├── mockData.ts
│   │   ├── utils.ts
│   │   └── healthCheck.ts
│   ├── types/            # TypeScript definitions
│   └── *.tsx             # Feature components
│
├── public/               # Static assets
│   ├── icons/           # PWA icons
│   ├── manifest.json    # PWA manifest
│   ├── offline.html     # Offline page
│   ├── robots.txt       # SEO
│   └── sitemap.xml      # SEO
│
├── src/                  # Legacy (transitional)
│   └── App.tsx          # Main app component
│
├── .github/
│   └── workflows/
│       └── ci.yml       # CI/CD pipeline
│
├── Configuration Files
│   ├── next.config.mjs
│   ├── tsconfig.json
│   ├── tailwind.config.ts
│   ├── postcss.config.mjs
│   ├── eslint.config.mjs
│   └── .prettierrc.json
│
└── Documentation
    ├── README.md
    ├── MIGRATION_SUMMARY.md
    └── ARCHITECTURE.md (this file)
```

---

## Component Hierarchy

```
App (Client Component)
│
├── Sidebar
│   ├── Logo
│   ├── Navigation
│   │   └── NavItems[]
│   └── Footer
│       ├── Settings Button
│       └── Logout Button
│
├── Main Content Area
│   ├── Mobile Header
│   │   ├── Menu Button
│   │   └── Chat Button
│   │
│   └── View Router
│       ├── Dashboard
│       │   ├── DashboardStats
│       │   └── VehicleTable (recent)
│       │
│       ├── Vehicles
│       │   └── VehicleTable (all)
│       │
│       ├── Add/Edit Vehicle
│       │   └── VehicleForm
│       │
│       ├── Vehicle Detail
│       │   ├── VehicleHistory
│       │   └── WorkEntryForm
│       │
│       ├── Staff Management
│       │   └── StaffManagement
│       │
│       ├── Shift Management
│       │   └── ShiftManagement
│       │
│       ├── Reservations
│       │   └── ReservationUpload
│       │
│       ├── Leave Requests
│       │   └── LeaveRequests
│       │
│       ├── Team Chat
│       │   └── TeamChat
│       │
│       ├── Health Check
│       │   └── HealthCheck
│       │
│       ├── Bulk Operations
│       │   └── BulkOperations
│       │
│       └── Reports
│           └── ReportsAnalytics
│
└── ChatBot (Floating)
    └── AI Assistant UI
```

---

## PWA Capabilities

```
┌────────────────────────────────────┐
│      Progressive Web App           │
└────────────────────────────────────┘
                │
    ┌───────────┼───────────┐
    │           │           │
    ▼           ▼           ▼
┌────────┐  ┌────────┐  ┌────────┐
│Install.│  │ Offline│  │ Push   │
│  able  │  │ Support│  │ Notif. │
└────────┘  └────────┘  └────────┘
    │           │           │
    └───────────┼───────────┘
                ▼
        ┌──────────────┐
        │ Service      │
        │ Worker       │
        └──────────────┘
                │
    ┌───────────┼───────────┐
    │           │           │
    ▼           ▼           ▼
┌────────┐  ┌────────┐  ┌────────┐
│ Cache  │  │ Precch.│  │Runtime │
│Strategy│  │ Assets │  │ Cache  │
└────────┘  └────────┘  └────────┘
```

---

This architecture provides:

- ✅ Scalable component structure
- ✅ Type-safe development
- ✅ Optimized build pipeline
- ✅ PWA capabilities
- ✅ Modern deployment flow
- ✅ Automated quality checks
